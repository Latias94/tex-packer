[workspace]
# Binary distribution is intentionally limited to the CLI. Rust crates are
# published to crates.io by the separate publish workflow.
members = ["cargo:crates/tex-packer-cli"]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# release.yml is generated first and then hardened by tools/update_dist_workflow.py.
allow-dirty = ["ci"]
# Packages must opt into binary distribution explicitly.
dist = false
# Avoid Cargo's workspace-wide build, which would compile the GUI and pull its
# platform dependencies into CLI release jobs.
precise-builds = true
# CI backends to support
ci = "github"
# Validate the tag before cargo-dist starts any artifact work.
plan-jobs = ["./release-tag-check"]
# The installers to generate for each app
installers = ["shell", "powershell"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# Which actions to run on pull requests
pr-run-mode = "skip"
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Whether to install an updater program
install-updater = false
# Where to host releases
hosting = "github"

[dist.github-custom-runners]
global = "ubuntu-24.04"
aarch64-apple-darwin = "macos-15"
x86_64-apple-darwin = "macos-15-intel"
x86_64-unknown-linux-gnu = "ubuntu-24.04"
x86_64-pc-windows-msvc = "windows-2025"
