# Journal — ADP-020 Algorithm Invariants

Date: 2026-05-22
Task: ADP-020

## Summary

Added `crates/tex-packer-core/tests/algorithm_invariants.rs` as a reusable behavior-level invariant Module. The tests exercise public offline layout and runtime session Interfaces instead of private implementation details.

## Behaviors Covered

- Offline algorithms satisfy shared frame invariants.
- Runtime strategies satisfy shared frame invariants.
- Multi-page cases assert page-local disjointness.
- Frame bounds stay within page dimensions.
- Source metadata and rotation-adjusted frame dimensions stay consistent.
- Repeated runs are deterministic.

## Evidence

- `cargo nextest run -p tex-packer-core --test algorithm_invariants` passed 4/4.
- `cargo fmt --check` passed.

## Next

Proceed to ADP-030 and use this harness as a regression safety net.
