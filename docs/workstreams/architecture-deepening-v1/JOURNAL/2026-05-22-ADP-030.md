# Journal — ADP-030 Pixel Geometry

Date: 2026-05-22
Task: ADP-030

## Summary

Deepened `geometry.rs` into the central internal Module for exclusive-edge and area semantics while preserving the public `Rect` shape and inclusive `right()`/`bottom()` methods.

## Changes

- Added crate-private helpers: exclusive edges, containment, intersection, widened area scoring, and overlap lengths.
- Replaced ad hoc geometry helpers in MaxRects, Guillotine, Skyline/WasteMap, Runtime, and page sizing where practical.
- Kept free-list extraction out of scope for ADP-040.

## Evidence

- `cargo fmt --check` passed after formatting.
- `cargo nextest run -p tex-packer-core --test algorithm_invariants --test boundary_conditions --test skyline_no_rotation --test runtime_skyline` passed 38/38.
- `cargo check -p tex-packer-core` passed.

## Next

Proceed to ADP-040 and extract shared free-space behavior behind `free_space.rs`.
