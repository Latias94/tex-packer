# Journal — ADP-040 Free-space Module

Date: 2026-05-22
Task: ADP-040

## Summary

Added `crates/tex-packer-core/src/free_space.rs` as a crate-private deep Module for shared free-space behavior.

## Changes

- Centralized Guillotine score and split helpers.
- Centralized rectangle subtraction, contained-rectangle pruning, adjacent merge, and non-empty push helpers.
- Reused helpers from Guillotine, MaxRects, Skyline WasteMap, and Runtime Guillotine paths while keeping algorithm-specific policy at call sites.

## Evidence

- `cargo fmt --check` passed.
- `cargo check -p tex-packer-core` passed.
- `cargo nextest run -p tex-packer-core --test algorithm_invariants --test maxrects_determinism --test skyline_waste_map --test runtime_session` passed 8/8.

## Next

Proceed to ADP-050 and move runtime placement strategy state/helpers out of `runtime.rs`.
