# Journal — ADP-060 Validated Packing Context

Date: 2026-05-22
Task: ADP-060

## Summary

Added crate-private `PackingContext` in `geometry.rs` to own derived packing invariants.

## Changes

- Centralized usable area, reserved-size extra, frame offset, trailing page-size extra, and page-size policy.
- Packer constructors and runtime placement now use shared usable-area derivation.
- Offline page sizing now delegates to `PackingContext`.
- Public `PackerConfig` and builder ergonomics stayed unchanged.

## Evidence

- `cargo fmt --check` passed.
- `cargo check -p tex-packer-core` passed.
- `cargo nextest run -p tex-packer-core --test boundary_conditions --test force_max_and_border --test padding_extrude_offsets --test algorithm_invariants` passed 26/26.

## Next

Proceed to ADP-070 and deepen offline pipeline internals.
