# Journal — ADP-070 Offline Pipeline Module

Date: 2026-05-22
Task: ADP-070

## Summary

Deepened offline pipeline internals by adding `OfflinePipeline` as an internal Module for image/layout packing phases.

## Changes

- `OfflinePipeline` now owns pack pages, image output construction, and layout atlas construction.
- Public `pack_images`, `pack_layout`, and `pack_layout_items` Interfaces stayed unchanged.
- Removed the now-unused `pack_prepared_pages` helper.

## Evidence

- `cargo check -p tex-packer-core` passed.
- `cargo nextest run -p tex-packer-core --test layout_vs_images --test pack_stats --test compose_extrude_no_bleed --test algorithm_invariants` passed 18/18.
- `cargo fmt --check` passed.

## Next

Proceed to ADP-080 and converge CLI/YAML/GUI configuration adapters.
