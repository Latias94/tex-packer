# Journal — ADP-080 Configuration Adapter Convergence

Date: 2026-05-22
Task: ADP-080

## Summary

Moved CLI, YAML, and bench configuration construction into a dedicated CLI adapter Module.

## Changes

- Added `crates/tex-packer-cli/src/config_adapter.rs`.
- Centralized string-to-enum parsing for algorithm family, heuristics, sort order, auto mode, and transparent policy.
- Removed silent YAML enum fallback; invalid config strings now return contextual errors.
- Routed pack and bench config creation through `PackerConfig::validate()`.
- Preserved existing CLI flags, including the historical `--mr-reference` force-on override after YAML merge.
- Left GUI presets unchanged because they already build typed `PackerConfig` values instead of parsing stringly config.

## Evidence

- `cargo fmt --check` passed.
- `cargo check -p tex-packer-cli -p tex-packer-gui` passed.
- `cargo nextest run -p tex-packer-core --test boundary_conditions` passed 17/17.

## Next

Proceed to ADP-090, run full closeout gates, update evidence, and commit the complete lane.
