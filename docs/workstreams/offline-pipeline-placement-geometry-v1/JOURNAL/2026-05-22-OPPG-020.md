# OPPG-020 journal — 2026-05-22

## Task

Replace duplicated pack_images / pack_layout / pack_layout_items placement loops with one shared offline pipeline Module while preserving public behaviour.

## Changes

- Introduced generic PreparedItem<T> for image and layout-only inputs.
- Introduced PackedFrame and PackedPage to keep internal item-index locality while producing public Page / Frame output.
- Extracted shared sorting into sort_prepared.
- Extracted shared placement into pack_prepared_pages and pack_pages_for_family.
- Extracted concrete-family packer construction into create_packer.
- Extracted atlas/meta construction into uild_atlas.
- Extracted image compositing into ender_output_page / uild_pack_output.
- Reworked Auto selection to operate on Vec<PackedPage> rather than full PackOutput, so image and layout-only paths share candidate selection.

## Validation

- cargo check -p tex-packer-core — passed.
- cargo fmt --check — passed.
- cargo nextest run -p tex-packer-core layout_vs_images pack_stats boundary_conditions padding_extrude_offsets — passed 8/8.
- cargo nextest run -p tex-packer-core — passed 91/91.

## Notes

This completes the first deepening step. The next useful seam is placement geometry: reserved size, half-padding offset, extrusion, and rotated frame dimensions are still repeated in Skyline, MaxRects, Guillotine, and runtime.
