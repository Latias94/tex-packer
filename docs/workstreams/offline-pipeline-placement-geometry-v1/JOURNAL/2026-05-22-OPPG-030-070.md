# OPPG-030 through OPPG-070 journal — 2026-05-22

## Tasks

- OPPG-030: Add regression tests for shared image/layout geometry.
- OPPG-040: Extract placement geometry.
- OPPG-050: Reuse geometry in runtime append.
- OPPG-060: Update docs and evidence.
- OPPG-070: Close the lane.

## Changes

- Added `crates/tex-packer-core/src/geometry.rs` with crate-private `PlacementGeometry`.
- Updated Skyline, MaxRects, Guillotine, and runtime append to use shared reserved-size and frame-construction logic.
- Added public-seam tests in `layout_vs_images.rs` covering forced rotation, trimmed image vs layout item agreement, padding/extrusion/page sizing, multi-page spill, and out-of-space progress.
- Added runtime regression coverage for rotated frame metadata.
- Updated README and core README with shared-pipeline and rotated-frame metadata notes.
- Completed the workstream ledger and closeout documentation.

## Validation

- `cargo nextest run -p tex-packer-core --test skyline_rotation_fit --test maxrects_rotation_fit --test guillotine_rotation_fit --test padding_extrude_offsets` — passed 6/6.
- `cargo nextest run -p tex-packer-core --test runtime_api_improvements --test runtime_atlas_tests --test runtime_session --test runtime_skyline --test runtime_shelf` — passed 46/46.
- `cargo nextest run -p tex-packer-core --test layout_vs_images --test padding_extrude_offsets --test force_max_and_border --test pow2_square` — passed 15/15.
- `cargo fmt --check` — passed.
- `cargo check -p tex-packer-core` — passed.
- `cargo nextest run -p tex-packer-core` — passed 96/96.
- `cargo nextest run --workspace` — passed 96/96.

## Notes

- The initially documented bare nextest filters matched 0 tests, so authoritative gates now use `--test` selectors.
- Runtime rotated-frame dimensions are now consistent with offline algorithms and the `Frame` model documentation.
