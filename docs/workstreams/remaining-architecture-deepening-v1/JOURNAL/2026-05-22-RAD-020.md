# Journal — RAD-020 Export Manifest Projection

Date: 2026-05-22
Task: RAD-020

## Summary

Introduced a shared export manifest projection Module for Atlas-to-export metadata fields.

## Changes

- Added `crates/tex-packer-core/src/export_manifest.rs`.
- JSON hash and JSON array exporters now render from `ExportManifest`.
- Plist exporters now render through one shared plist renderer over `ExportManifest`.
- CLI template context now uses `tex_packer_core::to_template_context()` instead of duplicating frame/source/pivot projection.
- Public export function names and current metadata shapes were preserved.

## Evidence

- `cargo nextest run -p tex-packer-core --test export_smoke` passed 1/1.
- `cargo check -p tex-packer-cli` passed.
- `cargo nextest run -p tex-packer-core --test layout_vs_images --test export_smoke` passed 6/6.

## Next

Proceed to RAD-030 and split runtime placement strategies into separate internal Adapter Modules.
