# Journal — RAD-040 Image Preparation Module

Date: 2026-05-22
Task: RAD-040

## Summary

Extracted image/layout preparation semantics out of `pipeline.rs` into a crate-private preparation Module.

## Changes

- Added `crates/tex-packer-core/src/preparation.rs`.
- Moved `PreparedItem<T>` into preparation.
- Moved trim rect calculation, transparent-policy handling, layout-only preparation, layout-item preparation, and deterministic sorting into preparation.
- Re-exported `compute_trim_rect` to preserve its public availability through `tex_packer_core::*`.
- Kept `pack_images`, `pack_layout`, and `pack_layout_items` behavior aligned.

## Evidence

- `cargo fmt --check` passed.
- `cargo nextest run -p tex-packer-core --test transparent_policy --test layout_vs_images --test padding_extrude_offsets --test algorithm_invariants` passed 13/13.
- `cargo check -p tex-packer-core` passed.

## Next

Proceed to RAD-050 and split CLI pack command orchestration into deeper internal Modules.
